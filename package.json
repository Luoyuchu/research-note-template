{
  "name": "research-note-template",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Single-repo Obsidian vault template with Quartz publishing and Obsidian Asset Relay integration hooks.",
  "engines": {
    "node": ">=22",
    "npm": ">=10"
  },
  "scripts": {
    "postinstall": "npm --prefix .site install",
    "sync:content": "node scripts/sync-vault-to-site.mjs",
    "build": "npm run sync:content && npm --prefix .site run quartz -- build",
    "dev": "node scripts/dev-site.mjs",
    "check:site": "npm --prefix .site run check",
    "build:secure": "npm run build && node scripts/protect-site.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "staticrypt": "^3.5.4"
  }
}
